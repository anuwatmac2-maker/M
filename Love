<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>I LOVE YOU 💖</title>

<style>
body {
    margin: 0;
    height: 100vh;
    background: radial-gradient(circle, #ffe4ec, #ffb6c1);
    display: flex;
    justify-content: center;
    align-items: center;
    overflow: hidden;
    font-family: 'Comic Sans MS', cursive;
}

/* หัวใจ */
.heart {
    font-size: 140px;
    cursor: pointer;
    animation: beat 1s infinite;
    user-select: none;
}

@keyframes beat {
    0%,100% { transform: scale(1); }
    50% { transform: scale(1.35); }
}

/* ข้อความตัวใหญ่มาก */
.big-love {
    position: absolute;
    font-size: 80px;
    color: #ff1493;
    text-shadow: 0 0 20px white;
    animation: showLove 2s ease-out forwards;
}

@keyframes showLove {
    0% {
        opacity: 0;
        transform: scale(0.5);
    }
    100% {
        opacity: 1;
        transform: scale(1);
    }
}

/* หัวใจลอย */
.floating-heart {
    position: absolute;
    font-size: 22px;
    animation: rise 4s linear forwards;
}

@keyframes rise {
    0% { opacity: 1; transform: translateY(0); }
    100% { opacity: 0; transform: translateY(-600px); }
}

/* ระยิบระยับ */
.sparkle {
    position: absolute;
    width: 8px;
    height: 8px;
    background: white;
    border-radius: 50%;
    box-shadow: 0 0 12px pink;
    animation: sparkle 1.3s forwards;
}

@keyframes sparkle {
    0% { opacity: 1; transform: scale(1); }
    100% { opacity: 0; transform: scale(0); }
}
</style>
</head>

<body>

<!-- ตอนแรกมีแค่หัวใจ -->
<div class="heart" onclick="showLove()">❤️</div>

<audio id="loveSong">
    <source src="https://cdn.pixabay.com/download/audio/2022/03/15/audio_2f8f5e4c6d.mp3">
</audio>

<script>
function showLove() {

    // เล่นเพลง
    const song = document.getElementById("loveSong");
    song.currentTime = 0;
    song.play();

    // ลบข้อความเก่าถ้ามี
    const old = document.querySelector(".big-love");
    if (old) old.remove();

    // ข้อความตัวใหญ่
    const text = document.createElement("div");
    text.className = "big-love";
    text.innerText = "I LOVE YOU 💖";
    text.style.top = "35%";
    text.style.left = "50%";
    text.style.transform = "translateX(-50%)";
    document.body.appendChild(text);

    // หัวใจลอย
    for (let i = 0; i < 18; i++) {
        const heart = document.createElement("div");
        heart.className = "floating-heart";
        heart.innerText = "💗";
        heart.style.left = Math.random() * window.innerWidth + "px";
        heart.style.bottom = "0px";
        heart.style.fontSize = (Math.random() * 30 + 15) + "px";
        document.body.appendChild(heart);
        setTimeout(() => heart.remove(), 4000);
    }

    // ระยิบระยับ
    for (let i = 0; i < 30; i++) {
        const sparkle = document.createElement("div");
        sparkle.className = "sparkle";
        sparkle.style.left = Math.random() * window.innerWidth + "px";
        sparkle.style.top = Math.random() * window.innerHeight + "px";
        document.body.appendChild(sparkle);
        setTimeout(() => sparkle.remove(), 1300);
    }
}
</script>

</body>
</html>
